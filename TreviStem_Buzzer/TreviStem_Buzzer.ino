#include "TreviStem_Buzzer.h"
const int buzzer = 8; //Define pin 10, can use other PWM pins  (5,6 or 9)
                       //Note pins 3 and 11 can't be used when using the tone function in Arduino Uno
const float songspeed = 1; //Change to 2 for a slower version of the song, the bigger the number the slower the song

void setup() {
  
}

void loop() {
}
